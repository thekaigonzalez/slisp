; ; The great pyramid!

; ; mid

; (defun create_pyramid (rows)
;   ; Don't hate me, this is directly
;   ; Stolen from the C version. lol
;   (progn
;     (set i 1)
    
;     (while (< i rows)
;       (set r 1)

;       (while (> r i)
;         (write-line "* ")
;         (set r (+ r 1)))
        
;       (set i (+ i 1)))))

; (create_pyramid 5)